# 변경 이력

이 문서는 Worklane의 릴리스별 주요 변경사항을 기록한다. 버전 표기는 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## v0.1.8

### 새 기능

- **전역 키보드 단축키**: `⌘0` 오버뷰, `⌘1`~`⌘9` 작업환경 점프, `⌘⌥↑↓` 순환, `⌘⇧A` 다음 주의 필요 항목, `⌘B`·`⌘⌥B` 패널 토글, `⌘N`·`⌘⇧N`·`⌘⇧T` 생성, `⌘,` 설정을 추가했다. `⌘/`로 전체 목록을 볼 수 있다.
- **사이드바 상태 롤업·접기·거르기**: 프로젝트 헤더에 상태별 개수를 요약하고, 프로젝트 섹션을 접을 수 있으며, 이름·경로·브랜치로 목록을 거를 수 있다.
- **명령 팔레트 개편**: 부분 일치 대신 퍼지 검색으로 바꾸고 최근 방문·주의 필요 항목을 위로 올린다. 명령/작업환경 구역과 단축키를 함께 표시한다.

### 개선

- **글자 크기 체계 정비**: 화면마다 제각각이던 글자 크기를 6단계로 통합하고 최소 크기를 올려 판독성을 높였다.
- **키보드 포커스 표시**: 어떤 화면에서도 현재 포커스 위치가 보이도록 전용 포커스 링을 도입했다.
- **첫 실행 안내**: 프로젝트가 없을 때 무엇을 해야 하는지 안내하고 그 자리에서 바로 추가할 수 있다.
- **프로젝트 추가 경로 확대**: 사이드바를 닫아 두어도 오버뷰와 명령 팔레트에서 프로젝트를 추가할 수 있고, 프로젝트 경로를 직접 입력·붙여넣을 수 있다.
- **작업환경 헤더 레이아웃**: 사이드바와 파일 패널을 모두 열어도 헤더가 한 줄을 유지한다.

### 버그 수정

- 실패 상태의 배지와 버튼 글자색이 적용되지 않아 읽기 어렵던 문제를 수정했다.
- 라이트 모드에서 오류 색상의 대비가 기준에 미치지 못하던 문제를 수정했다.
- 대기 상태 타일의 터미널 미리보기가 라이트 모드에서 회색으로 흐려 보이던 문제를 수정했다.
- 오버뷰 타일과 사이드바 삭제 버튼을 키보드로 조작할 수 없던 문제를 수정했다.
- 모션 최소화 설정을 켠 환경에서 일부 애니메이션이 그대로 재생되던 문제를 수정했다.

### 자동 업데이트

- 실행 중인 앱은 GitHub Releases의 `latest.json`을 읽어 이 버전으로 자동 업데이트된다.

## v0.1.7

### 개선

- **실패 상태 분리**: 에이전트 프로세스의 실제 종료 코드를 반영해 비정상 종료를 완료가 아닌 실패로 표시한다.
- **CLI 사전 검사**: 작업환경을 만들기 전에 실행 명령의 설치 여부를 확인해 즉시 안내한다.
- **오류 안내 개선**: 작업 실패의 실제 원인을 보존하고, 알려진 오류에는 해결 방법을 함께 표시한다.
- **기본 브랜치 자동 감지**: 프로젝트의 기본 브랜치를 저장하고 작업환경·팬아웃의 분기 기준 기본값으로 사용한다.
- **터미널 자동 포커스**: 작업환경이나 터미널 탭으로 전환하면 바로 입력할 수 있도록 포커스를 이동한다.

### 자동 업데이트

- 실행 중인 앱은 GitHub Releases의 `latest.json`을 읽어 이 버전으로 자동 업데이트된다.

## v0.1.6

### 새 기능

- **터미널 탭 작업환경**: 작업환경을 터미널 탭 방식으로 전환하고, 실행 중인 에이전트를 자동으로 감지한다.
- **Gajae Code 지원**: Gajae Code를 기본 에이전트 종류에 추가하고 SDK 브리지 기반 세션 상태 추적을 지원한다.
- **컨텍스트 메뉴**: 사이드바·파일 패널·미리보기·터미널 등 대상별 컨텍스트 메뉴와 에이전트 이름 변경 기능을 추가했다.

### 개선

- **에이전트 종류 순서 변경**: 설정에서 에이전트 종류를 드래그하여 순서를 바꿀 수 있다.
- **업데이트 자동 확인**: 앱 실행 중 새 버전을 주기적으로 확인한다.
- **작업환경 기본 이름**: 새 작업환경의 기본 이름으로 현재 브랜치 이름을 사용한다.
- 컨텍스트 메뉴 간 경합을 방지하고 키보드 접근성 및 작업 오류 안내를 보강했다.
- 화면 전체에 오버스크롤 바운스가 발생하지 않도록 개선했다.

### 자동 업데이트

- 실행 중인 앱은 GitHub Releases의 `latest.json`을 읽어 이 버전으로 자동 업데이트된다.

## v0.1.5

### 새 기능

- **다국어 지원(한국어·영어)**: UI 문자열을 다국어화하고, 설정 > 화면 > 언어에서 표시 언어를 전환할 수 있다. 기본 로케일은 한국어다.

### 개선

- **새 작업환경 자동 전환**: 작업환경을 새로 생성하면 해당 작업환경으로 자동 전환한다.

### 버그 수정

- 리소스 모니터의 CPU 사용률이 실제보다 튀어 보이던 문제를 수정했다.
- 루트 파일이 이름 없는 폴더로 렌더링되던 문제를 수정했다.

### 자동 업데이트

- 실행 중인 앱은 GitHub Releases의 `latest.json`을 읽어 이 버전으로 자동 업데이트된다.

## v0.1.4

### 새 기능

- **에이전트 종류 관리**: 앱 안에서 에이전트 종류(agent kind)를 직접 추가·수정·삭제할 수 있다. 설정에서 실행 명령과 표시 정보를 직접 구성한다.
- **IDE식 파일 트리**: 파일 뷰를 접고 펼 수 있는 폴더 트리로 표시해 변경 파일 탐색을 개선했다.
- **작업 이름 선택 입력**: 작업환경 생성 시 작업 이름을 선택값으로 바꿨다. 비워 두면 브랜치 이름을 기본값으로 사용한다.

### 개선

- **AI 사용량 표시 개편**: Claude·Codex 사용량을 잔여율로 표시하고, 최신 사용량을 직접 조회하도록 바꿨다. 사용량 수집 정확도도 함께 개선했다.
- **오버뷰 미리보기**: 오버뷰 그리드 타일 미리보기를 xterm 화면 스냅샷으로 렌더해 실제 화면과 일치하도록 했다.
- **사이드바 너비**: 좌측 사이드바 최소 너비를 15%에서 10%로 줄여 좁은 화면에서 활용도를 높였다.

### 버그 수정

- 설정 팝업의 사이드바 레이아웃이 흔들리던 문제를 수정했다.

### 자동 업데이트

- 실행 중인 앱은 GitHub Releases의 `latest.json`을 읽어 이 버전으로 자동 업데이트된다.
